import LayoutDefault from '../layout';

const Home = () => {
  return (
    <LayoutDefault>
      <h1 className="text-3xl font-semibold">Hello World!</h1>
    </LayoutDefault>
  );
};

export default Home;
